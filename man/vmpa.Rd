% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vmpa.R
\name{vmpa}
\alias{vmpa}
\title{Run VMPA}
\usage{
vmpa(
  input,
  context,
  algorithm = c("gsva", "fgsea"),
  gsva_score_scaling = c("none", "sample_z", "sample_pop_sd", "signature_z"),
  unique = TRUE,
  n = 250L,
  min_conf = 1L,
  targets = NULL,
  driver_filter = FALSE,
  return_gene_sets = FALSE,
  verbose = TRUE,
  seed = 123L,
  gsva_min_size = 1L,
  fgsea_min_size = 10L
)
}
\arguments{
\item{input}{Query input.
For \code{algorithm = "gsva"}, provide a numeric matrix or data frame with genes
in rows and samples in columns. Unique human gene symbols must be stored in
row names, and sample identifiers must be stored in column names.
For \code{algorithm = "fgsea"}, provide a named numeric vector of gene-level
statistics or rankings. Names must contain unique human gene symbols.
Gene-symbol matching is case-sensitive. Ensembl and Entrez identifiers are
not converted automatically.}

\item{context}{Character scalar. Cancer context to use. One of:
\code{"glioma"}, \code{"melanoma"}, \code{"nsclc"}, \code{"gastric"}, \code{"ovarian"},
\code{"crc"}, \code{"breast"}, \code{"prostate"}, \code{"pdac"}, or \code{"headneck"}.}

\item{algorithm}{Character scalar. Analysis workflow. Either \code{"gsva"} or
\code{"fgsea"}. Default: \code{"gsva"}.}

\item{gsva_score_scaling}{Character scalar. Optional post-processing applied
to GSVA-derived VMPA scores. One of \code{"none"}, \code{"sample_z"},
\code{"sample_pop_sd"}, or \code{"signature_z"}. Only used when
\code{algorithm = "gsva"}. Default: \code{"none"}.}

\item{unique}{Logical scalar. If \code{TRUE}, reduce results to one
target/protein-level output per target. If \code{FALSE}, keep all VMPA
signatures separately. Default: \code{TRUE}.}

\item{n}{Integer scalar. Number of bottom-ranked unique genes to include per
VMPA signature. Default: \code{250L}.}

\item{min_conf}{Integer scalar. Minimum confidence score required for a
VMPA signature to be included. Must be one of \code{1}, \code{2}, or \code{3}.
Default: \code{1L}.}

\item{targets}{Optional character vector. If provided, only signatures for
matching targets are retained. Default: \code{NULL}.}

\item{driver_filter}{Logical scalar. If \code{TRUE}, retain only signatures with
a non-\code{"None"} cancer-driver annotation. This is a broad annotation filter,
not a strict canonical-driver filter. Default: \code{FALSE}.}

\item{return_gene_sets}{Logical scalar. If \code{TRUE}, return a list containing
the VMPA result, the gene sets used for the analysis, and selection
metadata. Default: \code{FALSE}.}

\item{verbose}{Logical scalar. If \code{TRUE}, print progress messages. Default:
\code{TRUE}.}

\item{seed}{\code{NULL} or a single integer. Random seed applied locally to the
complete FGSEA backend call. Set to \code{NULL} to use the current global random
state without package-controlled seeding. Not used by GSVA. Default:
\code{123L}.}

\item{gsva_min_size}{Integer scalar. Minimum number of input genes represented
in a VMPA gene set for the GSVA backend. Default: \code{1L}, preserving the GSVA
default and the original VMPA GSVA workflow.}

\item{fgsea_min_size}{Integer scalar. Minimum number of finite ranked input
genes represented in a VMPA gene set for the FGSEA backend. Default: \code{10L},
preserving the original VMPA FGSEA workflow.}
}
\value{
If \code{return_gene_sets = FALSE}, a \code{vmpa_result} data frame.

For \code{algorithm = "gsva"}, rows correspond to targets/proteins when
\code{unique = TRUE} and to individual VMPA signatures when \code{unique = FALSE}.
Score columns correspond to samples.

For \code{algorithm = "fgsea"}, rows correspond to tested VMPA signatures and
columns contain FGSEA enrichment statistics together with VMPA metadata.

If \code{return_gene_sets = TRUE}, a list with:
\itemize{
\item \code{vmpa_result}: the \code{vmpa_result} data frame.
\item \code{gene_sets}: gene sets used for the analysis.
\item \code{signature_metadata}: metadata for the gene sets used.
\item \code{context}: selected cancer context.
\item \code{algorithm}: selected analysis workflow.
\item \code{unique}: whether signature reduction was used.
\item \code{unique_selection}: metadata describing selected signatures when
\code{unique = TRUE}; otherwise \code{NULL}.
}
}
\description{
\code{vmpa()} applies context-matched VMPA reference signatures to a user
query using either a GSVA-based or FGSEA-based workflow.
}
\details{
With \code{algorithm = "gsva"}, the input is a gene-by-sample expression matrix.
VMPA returns a \code{vmpa_result} data frame containing VMPA metadata
and one score column per sample.

With \code{algorithm = "fgsea"}, the input is a named numeric vector of gene-level
statistics or rankings. VMPA returns a \code{vmpa_result} data frame
containing FGSEA enrichment statistics together with VMPA metadata.

The selected cancer context determines which VMPA reference signatures are
used. Required reference data are resolved automatically by vmpaR.

VMPA reference data may contain multiple perturbation signatures for the
same target/protein.

With \code{unique = FALSE}, all signatures passing the selected filters are kept
as separate features.

With \code{unique = TRUE}, signatures are grouped by their target/protein
annotation (\code{cmap_name}). If validation metadata are available, validated
signatures are prioritized. Among the remaining candidates, signatures with
the highest \code{cps_conf_total} are retained.

For \code{algorithm = "gsva"}, all equally prioritized signatures for the same
target/protein are scored separately. If \code{gsva_score_scaling} is not
\code{"none"}, scaling is applied before target-level averaging. The final output
contains one row per target/protein.

For \code{algorithm = "fgsea"}, retained signatures are tested separately.
If multiple equally prioritized signatures remain for the same target/protein,
they are returned as separate FGSEA rows rather than being averaged or
arbitrarily collapsed.

The \code{seed} is applied locally around the complete FGSEA backend call. The
user's global random-number state is restored afterwards, including when the
backend raises an error. Set \code{seed = NULL} to disable package-controlled
seeding. The parameter has no effect when \code{algorithm = "gsva"}.

Input gene identifiers must be unique, case-sensitive human gene symbols
matching those used by the VMPA/LINCS reference (for example \code{TP53}, \code{EGFR},
or \code{AKT1}). Ensembl and Entrez identifiers are not converted automatically.
Missing, blank, whitespace-padded, or duplicated input symbols are rejected.

Overlap is evaluated separately for every selected VMPA gene set after
non-finite FGSEA statistics have been removed. GSVA retains gene sets with at
least \code{gsva_min_size} represented genes; FGSEA uses \code{fgsea_min_size}.
If no input symbol matches or no gene set reaches the relevant threshold,
VMPA stops before calling the backend. With \code{verbose = TRUE}, VMPA reports
the global overlap, threshold exclusions, and the number of sets actually
used by the backend.

Duplicate symbols in the VMPA reference are handled during gene-set
construction: the occurrence with the lowest reference-signature value is
retained, and a symbol counts at most once toward \code{n}.
}
\examples{
\dontrun{
if (requireNamespace("Biobase", quietly = TRUE)) {
  data(kebir_gb, package = "vmpaR")

  # GSVA workflow
  expr_mat <- Biobase::exprs(kebir_gb)

  gsva_result <- vmpa(
    input = expr_mat,
    context = "glioma",
    algorithm = "gsva"
  )

  # FGSEA workflow
  sample_metadata <- Biobase::pData(kebir_gb)

  relapsed <- sample_metadata$relapse_TYPE != "n"
  naive <- sample_metadata$relapse_TYPE == "n"

  stats_vec <- rowMeans(expr_mat[, relapsed, drop = FALSE]) -
    rowMeans(expr_mat[, naive, drop = FALSE])

  stats_vec <- stats_vec[is.finite(stats_vec) & !is.na(stats_vec)]
  stats_vec <- sort(stats_vec, decreasing = TRUE)

  fgsea_result <- vmpa(
    input = stats_vec,
    context = "glioma",
    algorithm = "fgsea"
  )
}
}

}
