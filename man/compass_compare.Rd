% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compass_compare.R
\name{compass_compare}
\alias{compass_compare}
\title{Compare COMPASS score patterns across experimental groups}
\usage{
compass_compare(
  compass_res,
  sample_info,
  sample_col = "sample",
  group_col = "group",
  contrast_table = NULL,
  mode = c("pairwise", "vs_control"),
  contrast = NULL,
  control_group = NULL,
  case_groups = NULL,
  keep_conf = NULL,
  include_effect_size = TRUE,
  protein_aggregation = c("none", "by_conf", "all_conf"),
  include_summary = TRUE,
  print_n = 10L
)
}
\arguments{
\item{compass_res}{Numeric matrix-like COMPASS result object with
pathways/signatures in rows and samples in columns.}

\item{sample_info}{Data frame describing the samples in \code{compass_res}.
Must contain at least:
\itemize{
\item one sample identifier column
\item one grouping column used for comparison
}}

\item{sample_col}{Character scalar. Column in \code{sample_info} containing sample
names that must match \code{colnames(compass_res)}.}

\item{group_col}{Character scalar. Column in \code{sample_info} defining the
comparison groups.}

\item{contrast_table}{Optional data frame defining comparisons explicitly.
Must contain:
\itemize{
\item \code{case_group}
\item \code{control_group}
Optional:
\item \code{comparison_id}
}

If \code{contrast_table} is supplied, it takes precedence over \code{mode},
\code{contrast}, \code{control_group}, and \code{case_groups}.}

\item{mode}{Character scalar. One of \code{"pairwise"} or \code{"vs_control"}.
Used only if \code{contrast_table} is \code{NULL}.}

\item{contrast}{Character vector of length 2 for \code{mode = "pairwise"}.
Order matters:
\itemize{
\item \code{contrast[1]} = case
\item \code{contrast[2]} = control
}}

\item{control_group}{Character scalar for \code{mode = "vs_control"}.}

\item{case_groups}{Optional character vector of case groups for
\code{mode = "vs_control"}. If \code{NULL}, all groups except \code{control_group} are used.}

\item{keep_conf}{Optional numeric/integer vector of confidence levels to keep,
e.g. \code{c(2, 3)}. Default: \code{NULL} (keep all).}

\item{include_effect_size}{Logical. If \code{TRUE}, compute a simple standardized
effect size based on pooled SD where possible. Default: \code{TRUE}.}

\item{protein_aggregation}{Character scalar. One of:
\itemize{
\item \code{"none"}: no protein aggregation
\item \code{"by_conf"}: aggregate by protein \emph{and} confidence level
\item \code{"all_conf"}: aggregate by protein across all confidence levels
Default: \code{"none"}.
}}

\item{include_summary}{Logical. If \code{TRUE}, print a compact console summary.}

\item{print_n}{Integer. Number of top positive/negative rows shown per
comparison in the console summary. Default: 10.}
}
\value{
A list with:
\itemize{
\item \code{compare_info}: data frame describing the realized comparisons
\item \code{results_signature}: full signature-level comparison table
\item \code{results_protein}: optional protein-level aggregation table or \code{NULL}
}
}
\description{
\code{compass_compare()} performs explicit group-wise comparisons on COMPASS score
matrices, typically produced by \code{compass(..., mode = "gsva")}.
}
\details{
The main output is a transparent signature-level result table. Optional
protein-level aggregation can be added explicitly, but is never silently used
as the main result.

Comparison specification can be done in two ways:
\enumerate{
\item \strong{Directly via \code{contrast_table}} (recommended for complex designs)
\item \strong{Convenience modes}:
\itemize{
\item \code{"pairwise"}: one case group versus one control group
\item \code{"vs_control"}: one control group versus several case groups
}
}
}
