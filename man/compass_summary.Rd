% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compass_summary.R
\name{compass_summary}
\alias{compass_summary}
\title{Summarize COMPASS results}
\usage{
compass_summary(
  compass_res,
  top_n = 100L,
  print_n = 20L,
  sample = NULL,
  expr_mat = NULL,
  index = NULL,
  include_rna_concordance = TRUE,
  include_variability = FALSE,
  include_signature_consistency = FALSE,
  include_hypothesis_shortlist = FALSE,
  shortlist_n = 3L,
  verbose = TRUE
)
}
\arguments{
\item{compass_res}{A COMPASS result object:
\itemize{
\item fgsea-like result data.frame
\item matrix of COMPASS activity scores
}}

\item{top_n}{Integer. Number of top entries returned per category.
Default: 100.}

\item{print_n}{Integer. Number of entries shown per section in the console.
Default: 20.}

\item{sample}{Optional character scalar. For matrix-like results, if provided,
positive/negative activity summaries are restricted to this sample/cell.
RNA–activity discordance will also be printed for this sample only, but is
still computed relative to the z-scored distribution across all overlapping
samples.}

\item{expr_mat}{Optional expression matrix (genes x samples). Required only if
\code{include_rna_concordance = TRUE} or \code{include_hypothesis_shortlist = TRUE}.}

\item{index}{Optional data.frame with pathway/protein metadata. If \code{NULL},
protein names are derived heuristically from pathway names.}

\item{include_rna_concordance}{Logical. If \code{TRUE}, compute RNA–activity
discordance for matrix-like results. Default: \code{TRUE}.}

\item{include_variability}{Logical. If \code{TRUE}, include top variable signatures
across all samples. Default: \code{FALSE}.}

\item{include_signature_consistency}{Logical. If \code{TRUE}, add an optional
protein-level warning block for convergent vs divergent signatures within
the selected sample. Currently supported for matrix-like COMPASS results.
Default: \code{FALSE}.}

\item{include_hypothesis_shortlist}{Logical. If \code{TRUE}, add an optional
hypothesis-generation shortlist based on signal strength, confidence,
consistency, and support. Default: \code{FALSE}.}

\item{shortlist_n}{Integer. Number of shortlist entries per quadrant in the
hypothesis matrix. Default: 3.}

\item{verbose}{Logical. If \code{TRUE}, print a compact console summary.
Default: \code{TRUE}.}
}
\value{
A data.frame with one row per summary entry.
}
\description{
Creates a compact summary table from a COMPASS result object and optionally
prints a readable summary to the console.
}
\details{
Supported result types:
\itemize{
\item fgsea-style data.frame returned by \code{compass(..., algorithm = "fgsea")}
\item score matrix returned by \code{compass(..., algorithm = "gsva")}
\item future AUCell score matrices should also work if they follow the same
basic matrix structure (gene sets x samples/cells)
}
}
