% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compass_annotate.R
\name{compass_annotate}
\alias{compass_annotate}
\title{Annotate COMPASS outputs with GO terms and CancerMine roles}
\usage{
compass_annotate(
  compass_obj,
  annotation_path = NULL,
  namespaces = NULL,
  evidence_codes = NULL,
  collapse_terms = TRUE,
  max_terms_per_namespace = 8L,
  use_go = TRUE,
  use_cancermine = TRUE,
  cancermine_path = NULL,
  context = NULL,
  cancermine_scope = c("all", "context", "both"),
  cancermine_cancer = NULL,
  cancermine_roles = NULL,
  cancermine_min_citations = 1L,
  max_cancermine_items = 5L
)
}
\arguments{
\item{compass_obj}{A COMPASS result object:
\itemize{
\item data.frame with \code{protein} and/or \code{pathway}
\item matrix with pathway names as rownames
}}

\item{annotation_path}{Optional path to \code{compass_go_annotations.rds}.
If \code{NULL}, the function checks \code{getOption("compass.go_annotation_path")}
and otherwise falls back to \code{annotation/compass_go_annotations.rds}
in the current working directory.}

\item{namespaces}{Optional character vector to filter GO namespaces.
Allowed values:
\itemize{
\item \code{"biological_process"}
\item \code{"molecular_function"}
\item \code{"cellular_component"}
Default: \code{NULL} (keep all).
}}

\item{evidence_codes}{Optional character vector of GO evidence codes to keep.
Default: \code{NULL} (keep all).}

\item{collapse_terms}{Logical. If \code{TRUE}, collapse GO terms per protein into
compact summary columns. Default: \code{TRUE}.}

\item{max_terms_per_namespace}{Integer. Maximum number of GO terms shown per
namespace when \code{collapse_terms = TRUE}. Default: 8.}

\item{use_go}{Logical. If \code{TRUE}, attach GO-based annotation. Default: \code{TRUE}.}

\item{use_cancermine}{Logical. If \code{TRUE}, attach CancerMine-based annotation.
Default: \code{TRUE}.}

\item{cancermine_path}{Optional path to \code{cancermine_collated.tsv}.
If \code{NULL}, the function checks \code{getOption("compass.cancermine_path")}
and otherwise falls back to
\code{annotation/annotation_raw/cancermine_collated.tsv}.}

\item{context}{Optional COMPASS context. One of:
\code{"breast"}, \code{"crc"}, \code{"gastric"}, \code{"glioma"}, \code{"headneck"},
\code{"melanoma"}, \code{"nsclc"}, \code{"ovarian"}, \code{"pdac"}, \code{"prostate"}.
Used only for context-filtered CancerMine annotation.}

\item{cancermine_scope}{One of \code{"all"}, \code{"context"}, \code{"both"}.
\itemize{
\item \code{"all"}: pan-cancer CancerMine summary
\item \code{"context"}: only context-filtered CancerMine summary
\item \code{"both"}: both pan-cancer and context-filtered summaries
Default: \code{"all"}.
}}

\item{cancermine_cancer}{Optional character vector. If provided, this
overrides automatic context mapping and keeps only these
\code{cancer_normalized} values.}

\item{cancermine_roles}{Optional character vector. If provided, keep only
selected roles such as \code{"Oncogene"}, \code{"Tumor_Suppressor"}, \code{"Driver"}.
Default: \code{NULL} (keep all roles).}

\item{cancermine_min_citations}{Integer. Minimum \code{citation_count} required to
keep a CancerMine row. Default: 1.}

\item{max_cancermine_items}{Integer. Maximum number of roles/cancers shown
in collapsed CancerMine annotation strings. Default: 5.}
}
\value{
If \code{compass_obj} is a data.frame and \code{collapse_terms = TRUE}:
the same data.frame plus annotation columns.

If \code{compass_obj} is a matrix and \code{collapse_terms = TRUE}:
a list with:
\itemize{
\item \code{data}: the original matrix
\item \code{row_annotation}: one row per matrix row/pathway
}

If \code{collapse_terms = FALSE}:
a list with:
\itemize{
\item \code{data}: the original object
\item \code{go_annotations}: the filtered long-format GO table (if requested)
\item \code{cancermine_annotations_all}: pan-cancer CancerMine rows (if requested)
\item \code{cancermine_annotations_context}: context-filtered CancerMine rows
(if requested)
}
}
\description{
Adds downstream annotation layers to COMPASS-derived objects.
}
\details{
Supported input types:
\itemize{
\item data.frame-like COMPASS outputs with a \code{protein} column
\item data.frame-like COMPASS outputs with a \code{pathway} column
\item matrix-like COMPASS outputs with pathway names as rownames
}
}
