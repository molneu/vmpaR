% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compass_compare_wide.R
\name{compass_compare_wide}
\alias{compass_compare_wide}
\title{Pivot COMPASS comparison results to a wide signature table}
\usage{
compass_compare_wide(
  compare_obj,
  comparison_ids = NULL,
  id_cols = c("pathway", "protein", "conf_total"),
  value_cols = c("delta"),
  comparison_col = "comparison_id",
  add_delta_summary = TRUE,
  sort_by = c("max_abs_delta", "mean_abs_delta", "none"),
  decreasing = TRUE
)
}
\arguments{
\item{compare_obj}{Either:
\itemize{
\item the full list returned by \code{compass_compare()}, or
\item a signature-level data.frame like \code{cmp$results_signature}
}}

\item{comparison_ids}{Optional character vector. If provided, restrict to
selected comparisons in this order.}

\item{id_cols}{Character vector defining the identifier columns that should
remain on the left side of the wide table. Default:
\code{c("pathway", "protein", "conf_total")}.}

\item{value_cols}{Character vector of columns to pivot wide. Default:
\code{c("delta")}.
Useful examples:
\itemize{
\item \code{c("delta")}
\item \code{c("delta", "direction")}
\item \code{c("delta", "mean_case", "mean_control")}
}}

\item{comparison_col}{Character scalar. Column holding comparison labels.
Default: \code{"comparison_id"}.}

\item{add_delta_summary}{Logical. If \code{TRUE} and \code{"delta"} is among
\code{value_cols}, add summary columns such as \code{max_abs_delta},
\code{mean_abs_delta}, \code{n_delta_pos}, \code{n_delta_neg}, and \code{delta_pattern}.
Default: \code{TRUE}.}

\item{sort_by}{Character scalar. One of:
\itemize{
\item \code{"max_abs_delta"}
\item \code{"mean_abs_delta"}
\item \code{"none"}
Default: \code{"max_abs_delta"}.
}}

\item{decreasing}{Logical. Sort descending if \code{TRUE}. Default: \code{TRUE}.}
}
\value{
A wide data.frame.
}
\description{
\code{compass_compare_wide()} converts signature-level output from
\code{compass_compare()} into a wide table so that multiple comparisons can be read
side by side for the same signature.
}
\details{
Typical use case:
\itemize{
\item compare \verb{Temsi_50 vs DMSO} and \verb{Temsi_500 vs DMSO} side by side
\item inspect whether the same signature shifts similarly or differently
\item create a practical work table for downstream filtering/export
}
}
